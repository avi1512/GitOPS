apiVersion: v1
kind: Service
metadata:
  labels:
    apps: nginx-webserver
  name: svc1
spec:
  selector:
    apps: web2
  type: NodePort
  ports:
    - name: myport
      port: 80
      nodePort: 32324

